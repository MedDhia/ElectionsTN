pdfplumber>=0.11
pypdfium2>=4
pytesseract>=0.3
Pillow>=10
anthropic>=1.0
# The PV readers: registration, thresholding and connected components.
# Floored rather than pinned -- nothing byte-checked depends on it.
opencv-python>=4.10

# The offline PV readers (`tools/decode_all.py`, `tools/decode_local_2023.py`).
# OpenCV recovers the printed grid of a scanned tally form; the digit classifier
# is a small CNN, and the CPU build of torch is all it needs — nothing here
# trains on a GPU, and nothing here downloads a model.
opencv-python-headless>=4.9
torch>=2.2

# The map tools. Pinned exactly, not floored, because every figure in `maps/`
# is committed and checked for byte-identity on re-render: matplotlib stamps its
# own version into PNG metadata and the PDF Producer string, so a minor upgrade
# rewrites all 528 files and drowns out the signal those checks exist to read.
# Upgrade deliberately, in a commit that does nothing else.
matplotlib==3.11.1
numpy==2.4.6
scipy==1.17.1
# Ward agglomeration under a contiguity constraint, for the electoral
# regions in tools/make_clusters.py. Deterministic given the same input,
# which is what lets those figures be byte-checked like the rest.
scikit-learn==1.9.0

# The surname dot maps in maps/surnames/ set their labels in Arabic, which
# matplotlib cannot shape: it has no HarfBuzz, so a label it sets comes out as
# unjoined letters in the wrong order. Pillow built against Raqm does shape it,
# which is why the labels are rendered through Pillow and placed as images, and
# why the font is a system package rather than a pip one:
#     apt-get install -y fonts-hosny-amiri
# Without the font the figures still build, with Latin labels and a printed note.
